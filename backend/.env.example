# VOO Kyamatu Ward - Environment Variables# PostgreSQL Database

# Copy this file to .env and fill in your valuesDATABASE_URL=postgresql://user:password@host:5432/database_name



# MongoDB Connection String (REQUIRED)# JWT Secret (generate a strong random string)

# Get this from MongoDB AtlasJWT_SECRET=your-super-secret-jwt-key-here

MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/database_name

# MongoDB Atlas (optional)

# Server Port (Optional - Render sets this automatically)MONGO_URI=mongodb+srv://user:password@cluster.mongodb.net/database?retryWrites=true&w=majority

PORT=4000

# Server Port (Render will set this automatically)

# Environment (production/development)PORT=4000

NODE_ENV=production

# Node Environment

# Admin Dashboard Port (for admin-dashboard.js)NODE_ENV=production

ADMIN_PORT=5000
